# Production dependencies for Railway deployment.
# For the full dev set (including geo ingestion), use requirements_v2.txt.

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Database & ORM
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.25
geoalchemy2>=0.14.0
psycopg2-binary>=2.9.9

# Redis cache
redis>=5.0.0

# LLM scoring
openai>=1.30.0
instructor==0.4.8

# GDELT / BigQuery (for ingestion scripts + aggregator)
google-cloud-bigquery>=3.17.0
google-cloud-bigquery-storage>=2.24.0
db-dtypes>=1.2.0
pyarrow>=15.0.0
pandas>=2.1.0

# Utilities
python-dotenv>=1.0.1
httpx>=0.25.0
tenacity>=8.2.3
tqdm>=4.66.0
